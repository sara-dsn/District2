{% extends 'base.html.twig' %}


{% block body %}
   

    <div class="d-none d-md-block ">

        <div class="col-12 d-flex justify-content-center">
            <h2 class="font-weight-bold title d-none d-md-block">Nos Catégories </h2>
        </div>
        <div class="w-100  row">
            <div class="col-3 d-none d-md-block  ">
                <div class="card border-0 bg-transparent">
                    <div class="card-body">
                        <img src="asset/cat.fond/cuisinier.png" class="card-img-top img-fluid" alt="Cuisinier">
                    </div>
                </div>
            </div>

            <div class="  row  col-11 col-md-8 d" >   
                {% for categories in categorie %}  
                {# {% if categories.active == 1 %}   autre maniere de faire         #}
                    <div class="t d-none d-md-block cat card custom-card col-12 col-md-3 ml-2 mt-2 mr-2 mb-4 ">
                        <img class=" card-img-top taille rounded img-fluid " src="asset/category/{{ categories.image }}" alt="{{ categories.libelle }}">
                        <div class="card-body  pl-3">
                            <h3 value="{{ categories.id }}" class=" titre card-title font-weight-bold  id">{{ categories.libelle }}</h3>
                            <span class="stock text-success mb-2">Disponible<span>
                        
                        </div>
                    </div>
                {# {% endif %} #}
                {% endfor %}
            </div>
    
            <div class="col-12 d-flex justify-content-center mb-5">
                <h2 class="font-weight-bold title font-italic mt-5 ml-5">Nos Best-sellers </h2>
            </div>
        
            <div class="w-100 d-flex justify-content-center row">
    
                <div class="row  col-10 col-md-8   ml-3 d-flex justify-content-center " >
                    {# <?php foreach ($best as $plat) {  ?>#}
                    {% for plt in plat %}
                        <div class="card col-12  pb-2 ml-1 mb-1 mr-1 col-md-3 position-relative ">
                            <img class="card-img-top mt-3 img-fluid rounded himg" src="asset/food/{{ plt.image }}" alt="{{ plt.libelle }}">
                            <div class="card-body ">
                                <h5 class="card-title font-weight-bold">{{ plt.libelle }}</h5>
                                <p class="card-text ">{{ plt.description }} <br> Menu: {{ plt.prix }} €</p><br> 
                            </div> 
                            <div class="  row d-flex justify-content-center">
                            <a href="{{ path('add',{'id':plt.id}) }}" id="btnplt" type="submit" value="{{ plt.id }}" class="btn  col-4 t"><img class="img-fluid " src="{{asset('asset/cat.fond/panierbtn.png')}}" alt="ajouter au panier"></a>        
                                <div><img class="img-fluid position-absolute  img-fluid col-5 mb-5 mr-5" src="asset/cat.fond/bestseller.png" alt="best seller"> </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>   
        </div >
    </div>
    <div class="w-100 d-flex justify-content-center d-none d-md-none">
        <h2 class="font-weight-bold  row">Nos Best-sellers </h2>
    </div>
    <div class=" row col-10 d-none d-md-none   ml-3 d-flex justify-content-center" > 
        {# <?php foreach ($best as $plat) { #}
        {% for plt in platsm %}
            <div class="card col-12 mt-3 pb-2 ml-1 mb-1 mr-1 col-md-3 position-relative">
                <img class="card-img-top img-fluid rounded himg" src="asset/food/{{ plt.image }}" alt="{{ plt.libelle}}">
                <div class="card-body ">
                    <h5 class="card-title font-weight-bold">{{ plt.libelle }}</h5>
                    <p class="card-text ">{{ plt.description }} <br> Menu: {{ plt.prix }} €</p><br>
                    {# <img class="img-fluid position-absolute  img-fluid " src="asset/cat.fond/bestseller.png" alt="best seller"> #}
                </div>
                <div class="mt-auto  text-center"> <a href="{{ path('add',{'id':plt.id}) }}" id="btnplt" type="submit" value="{{ plt.id }}" class="btn  col-4 t"><img class="img-fluid " src="{{asset('asset/cat.fond/panierbtn.png')}}" alt="ajouter au panier"></a></div>
            </div>
        {% endfor %}
    </div>
    <div class="d-flex justify-content-end mt-4">
        <a type="submit" href="{{path('app_categorie')}}" class="btn btn-dark t mr-2" >Suivant</a>
    </div>
{% endblock %}
